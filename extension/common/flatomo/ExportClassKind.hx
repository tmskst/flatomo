package flatomo;

/** 出力するシンボルの親クラス設定 */
enum ExportClassKind {
	/** シンボルは'flatomo.display.Container'として出力される */
	Container;
	/** シンボルは'flatomo.display.Animation'として出力される */
	Animation;
	/** シンボルは'flatomo.display.PartsAnimation'として出力される */
	PartsAnimation;
}
